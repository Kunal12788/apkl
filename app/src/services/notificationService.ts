export const sendActivityNotification = async (
  _action: 'login' | 'logout',
  _userEmail: string,
  _userName: string,
  _userRole: string
): Promise<void> => {
  // Activity notifications disabled
};

